name: markdownlint (disabled)

on:
  # 手動実行のみ（デフォルト無効）
  workflow_dispatch:

jobs:
  lint:
    if: ${{ false }} # 明示的に無効化
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DavidAnson/markdownlint-cli2-action@v16
        with:
          globs: |
            **/*.md
          config: .markdownlint.json
